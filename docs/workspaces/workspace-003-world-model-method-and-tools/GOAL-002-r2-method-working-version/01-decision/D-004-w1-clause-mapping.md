---
id: GOAL-002-r2-method-working-version
doc: decision-entry
record_id: D-004
status: accepted
parent: GOAL-001-world-model-method-and-tools
created: 2026-09-26
updated: 2026-09-26
version: 0.1.0
---

## D-004 · W1 条款映射与差异登记

### 触发

用户 2026-09-26 指令开始本目标 **W1**（条款映射与差异登记）：把需求原文 §4～§11 与 §13 逐条展开为「方法必须回答的问题清单」，标出处置与依据，并把需研究、需取舍的点登记为信息项或决策候选。

权威依据：需求**原文**（下游仓 `WorldModel.ModernCultivation` @ `b10cadc` 的 `exchange/WRK-002-world-model-method-and-tools/需求-2026-09-26.md`）；R1 冻结的九章结构与不覆盖边界（Root [`D-002-r1-freeze.md`](../../GOAL-001-world-model-method-and-tools/01-decision/D-002-r1-freeze.md)）；执行主体与协作约束（本目标 [`D-003`](D-003-executor-boundary-revision.md)）。

### 工作范围与差异登记

**W1 名义范围** = 需求原文 §4～§11 与 §13。下列三处差异须登记，避免"表外内容被漏掉"：

| 差异 | 内容 | 处置 |
|------|------|------|
| **E1** | 需求原文 §1～§3（尤其 §3「基本工作方式」的按需扩展六步与「没有真实裁决需求时，不主动扩展世界模型」）**不在 W1 名义范围内**，但它是方法的**主干流程**，不是可选项 | **登记为决策候选 `C6`**：是否把 §3 立为方法主文档的「总流程」章（置于 §4 之前），而非只作背景说明 |
| **E2** | 需求原文 §12「本需求不包含的内容」十一项，W1 不逐条重列为映射行 | 已由 R1 冻结第 1 项与原文 §12 覆盖；在方法主文档中**只需列为一节边界声明**，不产生方法步骤 |
| **E3** | §4.1（模型间依赖与组合关系的表达）、§5.1（能力发现清单）、§7.5（模型兼容与冲突检查）在"是否需要一处权威清单/索引"上**重叠** | **登记为决策候选 `C1`**：合并为方法内的一处要求，避免出现两套清单规则 |

### 条款映射表（§4～§11、§13）

> 「处置」列的取值：**直接采纳** / **需研究** / **需取舍** / **明确不覆盖**。
> 本表由助手整理（协作位 = 整理与提示）；标 **需取舍 / 需研究** 的判断属**创作者裁定点**，汇总为 `C1`～`C8` 并登记为信息项 `I-205`。**裁定前不得把候选结论写入方法正文。**

| 条款 | 方法必须回答的问题 | 处置 | 依据 / 说明 |
|------|--------------------|------|-------------|
| **§4.1** 世界模型描述运行机制，含四类内容（现实规律默认参考 / 虚构世界基本前提 / 条件-作用-结果机制模型 / 模型间依赖与组合关系）；回答「世界怎样运行」 | 四类内容如何区分与记录？「默认参考」如何裁剪与登记（借用现实知识时要不要写适用条件）？基本前提如何保持最少？模型间依赖与组合关系如何表达？ | 直接采纳（**依赖与组合的表达与 §7.5 合并，见 E3 / `C1`**） | 条款直接给出内容类别与用途；与下游 Charter「机制层」定位一致 |
| **§4.2** 世界状态记录当前具体事实（对象；位置、资源、能力、关系；环境参数；已发生并被接受的历史事实；其他实例值）；回答「现在具体是什么」；不得与普适机制混为一体 | 状态的粒度与最小结构？「已发生并**被接受**」由谁判定、如何留痕？**一条内容属机制还是状态，用什么可操作谓词判定**？ | 直接采纳（谓词的可操作性属 W2 内容；**参数归属的判定与 §4.3 / §6 合并，见 `C5`**） | 条款已给出两层职责划分；判定谓词是方法必须补上的可操作件 |
| **§4.3** 裁决结果 = 以世界状态为输入、调用相应模型得到的答案；可追溯：用了什么模型、什么状态、模型版本、适用范围、推导或求解依据 | 裁决结果的最小可追溯要素有哪些？纯文本下如何留痕而不变成强制 Schema（避免"表单代替建模"）？ | 直接采纳 | 条款列出五项可追溯要素，方法直接落为"一次裁决的最小记录" |
| **§5.1** 能力发现：回答「现有模型能回答哪些类型的问题、需要哪些输入」；至少说明 8 项（支持的问题类型、问题准确含义、需要哪些状态或参数、参数类型/单位/取值要求、使用哪些模型、适用条件、能达到什么精度、什么情况下无法裁决）；新增模型应表现为新增或增强某种可用裁决能力 | 能力清单**由谁维护、放在哪、何时更新**？「精度」如何声明（定性 / 区间 / 数值 / 分布）？新增模型如何体现为"能力增强"而不是"多一个文件"？ | 直接采纳（内容清单明确）；**清单承载与同步位置 = 需取舍 → `C1`** | 8 项共同构成"能力发现"的输出契约；承载位置原文未规定 |
| **§5.2** 客观裁决：给定受支持问题 + 当前状态 + 必要参数 + 要求精度，返回五类之一（确定结果 / 结果范围 / 有明确机制依据的不确定或随机结果 / 较低精度的可用结果 / 明确说明无法裁决）；不得为满足调用方而伪造不存在的精度或确定性 | 五类返回如何判定与记录？「明确说明无法裁决」如何接回缺口判定（闭环）？谁来判定"精度确实达不到"？ | 直接采纳 | 五类返回与 §10 缺口判定构成闭环；「不得伪造精度」是硬纪律 |
| **§6** 不把"定性模型"和"定量模型"视为两种独立模型；一个机制模型可能只给定性关系、可给区间或阈值、可用具体参数做数值计算、可描述有明确随机机制的结果分布；定量裁决本质是"把状态参数代入机制模型后求值"；具体参数属世界状态或当前裁决实例；模型应明确自己支持什么精度 | 同一模型的"精度阶梯"如何声明、何时升级？**参数属世界状态还是属当前裁决实例**如何判定？ | 直接采纳（**参数归属与 §4.2 / §4.3 合并，见 `C5`**） | 条款已给出"一种模型、多种精度"的方向；归属判定需可操作 |
| **§7.1** 机制优先于结果：回答"什么条件通过什么机制产生什么结果"，而不是记录"当前这个案例的答案"；模型应能处理一类问题，对象、地点或状态改变后仍可用 | 如何**检查**候选模型是"一类问题的机制"而不是"个案答案"（可操作检查项）？ | 直接采纳（检查项由 W2 给出） | 条款给出判准（换对象/地点/状态仍可用），方法需把它变成检查步骤 |
| **§7.2** 允许基本前提：虚构世界可以存在无法或无需继续向下解释的基本前提；可直接作为模型依据，但应尽量保持最少；能由现有机制推出的事实，不应再次声明为新的基本前提 | 基本前提在哪里登记？"最少"如何核对（逐条问"能否由现有机制推出"）？ | 直接采纳 | 条款给出核对方向；登记位置与 `C1`/`C8` 关联 |
| **§7.3** 允许未定义：当前创作尚未需要的问题可以保持未定义；"尚未建模"不等于"不存在" | "未定义"如何**显式记录**，以免以后被误读为"不存在"或被当成结论？ | 直接采纳；**登记位置 = 需取舍 → `C8`** | 若不显式登记，未定义会被静默当成"不存在"，违背条款 |
| **§7.4** 禁止针对结果补丁：不得因当前剧情希望某结果而临时增加有利机制、临时增加有利基本前提、临时补写此前不存在的关键状态、或在冲突模型中选择对当前剧情有利的一项 | 用什么**自检问句**发现"补丁"？**谁**作出该判断？ | 直接采纳；判断点 = **创作者**（与 [`D-003`](D-003-executor-boundary-revision.md) 的协作位一致；助手可给建议与反例，结论须由创作者作出） | 条款给出四种补丁形态，方法需给出对照检查 |
| **§7.5** 模型需要相互兼容：所有模型共同描述同一个世界；出现冲突时应检查 6 项（输入状态是否相同、适用范围、时间尺度、精度、多个机制是否需联合求解、是否存在模型版本变化）；不得用"特殊情况"绕过尚未解释的冲突 | 六项检查如何执行、由谁执行？**冲突登记在哪里**（是否指定一处登记，还是只要求"可登记"）？ | 直接采纳；**冲突登记位置 = 需取舍 → `C1` / `C8`** | 六项检查是方法可直接落地的内容；登记位置原文未规定 |
| **§8** 现实世界可作默认参考，但不能把个人常识直接等价于现实规律；物理、生理、工程等适用范围较稳定的问题可较直接借用现实知识；社会规范、组织行为、家庭关系、权力结构、声誉传播、群体决策等高度情境化问题，需明确适用的文化、制度、组织或其他条件，必要时建立专门机制，不能仅以"现实通常如此"完成裁决 | **"适用范围较稳定"与"高度情境化"如何划界**（可操作判据）？"必要时建立专门机制"的触发条件是什么？ | **需取舍 → `C2`** | 原文只给两组举例，未给可操作划界判据；给判据与只给举例+要求显式声明适用条件，是两种不同的方法，须创作者裁定 |
| **§9** 新模型最低要求 11 项（逐项归宿见**附表 A**） | 11 项如何落成**可直接填写的「模型条目最小结构」**（W3）？其中**哪些必填、哪些可标"待定"**？ | 直接采纳（逐项作为 W3 字段候选）；**必填/可空划分 = 需取舍 → `C3`** | 11 项全部设为必填会写出创作者无法一次填完的表单，反而诱发补丁或弃用 |
| **§9 末段** 初期允许使用自然语言模型，不要求所有机制数学化；结构化和工具化只在重复使用确有价值后进行 | 方法如何表述"先用自然语言"的许可与"何时升级结构化"的触发？ | 直接采纳 | 与 R1 冻结「本轮不引入程序化工具」一致；不把结构化设为前提 |
| **§10** 缺口判定五步：① 是否真的是客观世界问题；② 是否已存在相应裁决能力；③ 是否只是缺少当前世界状态；④ 是否可通过已有模型组合得到答案；⑤ 是否只是要求的精度超过现有模型能力；只有上述检查仍无法解决，才认定"存在新的世界模型能力缺口"并启动建模 | 五步如何落成可执行的「能力缺口判定清单」（W3）？第①步判定"不是客观世界问题"时如何**明确回绝**并回流？ | 直接采纳 | 五步次序已给出，且与 §5.1 能力发现天然衔接（先查能力，再查状态 / 组合 / 精度） |
| **§11** 世界模型允许随需求持续扩展和修订；修订后应能识别：哪些裁决能力受影响、哪些过去的裁决依赖旧版本、哪些裁决需要重新计算；世界模型只负责声明哪些客观裁决仍然有效、哪些已失效或需重新检查；具体作品如何处理依赖这些裁决的既有剧情，不属本方法职责 | 纯文本下如何保留"**旧裁决 ↔ 模型版本**"的依赖关系？"需重算"如何登记与回收？ | 直接采纳；**依赖记录粒度 = 需取舍 → `C4`** | 逐条记录旧裁决可追溯性最好但成本高；按能力段落记录成本低但会漏 |
| **§13** 十项指导 + 最低可用结果 + 末段（"如果现有要求中存在不合理假设，应指出具体问题并提出替代方案；不要求为了保持本文结构而保留没有实际价值的设计"） | 十项是否各有落点（逐项见**附表 B**）？"最低可用结果"如何作为**可执行判据**（W4 的判据）？对原文假设的**异议**如何登记？ | 直接采纳；**异议登记义务 = 需取舍 → `C7`** | 十项映射即 R1 冻结的第 9 章；末段是一项**授权**：方法可以对原文提出修正建议 |

#### 附表 A · §9 新模型最低要求 11 项的逐项归宿（→ W3 字段候选）

| # | 条款要求的项 | W3 字段候选 | 关联条款 |
|---|--------------|-------------|----------|
| 1 | 它解决哪一类客观问题 | 能力 / 问题类型 | §5.1、§10① |
| 2 | 为什么现有模型不足 | 缺口依据（五步结论） | §10②③④⑤ |
| 3 | 依赖哪些现实参考或基本前提 | 依赖（现实参考 / 基本前提） | §8、§7.2 |
| 4 | 需要哪些状态输入 | 状态输入 | §4.2 |
| 5 | 输入通过什么机制产生输出 | 机制（条件 → 作用 → 结果） | §4.1、§7.1 |
| 6 | 在什么范围内适用 | 适用范围 | §8 |
| 7 | 能够达到什么裁决精度 | 精度 | §6、§5.1 |
| 8 | 与其他模型如何组合 | 组合 / 依赖关系 | §4.1、§7.5 |
| 9 | 什么情况下应拒绝裁决 | 拒绝条件 | §5.2 |
| 10 | 经过哪些最小条件变化或反例检查 | 检查记录 | §7.1、§7.4 |
| 11 | 当前模型版本 | 版本 | §11 |

处置：**直接采纳**（11 项逐项落到 W3 字段候选）。必填 / 可空划分见 `C3`。

#### 附表 B · §13 十项指导的逐项归宿（→ W2 章节）

| # | §13 的指导项 | 落点（R1 冻结的九章结构） |
|---|--------------|---------------------------|
| 1 | 如何识别一个新的客观裁决需求 | 章 1（裁决需求与三概念）+ 章 7 第①步 |
| 2 | 如何通过能力发现判断现有模型能否处理 | 章 2（两类服务契约）+ 章 7 第②步 |
| 3 | 如何识别缺状态、缺组合、缺精度还是缺机制 | 章 7 第③④⑤步 |
| 4 | 如何定义新的裁决能力 | 章 2（能力发现的新增部分）+ 章 6 |
| 5 | 如何构造支持该能力的最低充分机制模型 | 章 6（§9 十一项） |
| 6 | 如何检查模型是否只是当前案例补丁 | 章 4（§7.1 / §7.4 检查） |
| 7 | 如何验证模型的基本适用范围和条件敏感性 | 章 6（适用范围 / 精度）+ 章 5（§8 适用条件） |
| 8 | 如何处理模型组合与冲突 | 章 4（§7.5）+ 章 1（依赖与组合关系） |
| 9 | 如何管理模型版本和受影响旧裁决 | 章 8（§11） |
| 10 | 如何将新增能力重新提供给实际创作使用 | 章 2（能力清单更新）+ 章 8（版本声明） |
| — | **最低可用结果**：针对一个真实创作中出现的客观问题，完整跑通"发现能力缺口 → 构建最低充分模型 → 注册能力 → 完成裁决 → 检查 → 返回创作"闭环 | **W4 的判据**（用 Root `I-002` 的真实问题执行） |

处置：**直接采纳**（十项各有落点，映射表本身即 R1 冻结的第 9 章）。

### 决策候选（须由创作者裁定；登记为 `I-205`）

> 以下为 W1 识别出的**方法设计选择**。助手只做整理与提示，**结论由创作者作出**（[`D-003`](D-003-executor-boundary-revision.md) 第 1～3 项）。裁定前不得把任一候选的倾向写入方法正文。

| ID | 候选问题 | 影响 | 助手倾向（仅作提示，非结论） |
|----|----------|------|------------------------------|
| **C1** | 是否要求方法内**指定一处权威的「能力 / 模型清单」**（含位置与更新时机），还是只要求"能被找到即可"？涉及 §4.1 / §5.1 / §7.5 三处重叠 | 方法是否引入"索引"这一结构；与下游承载未知 `U-5`（是否单设系统索引）相邻 | 倾向**指定一处**并要求更新时机，否则"能力发现"没有可查之处 |
| **C2** | §8 的划界：给出**可操作的"适用范围稳定性"判据**，还是只给举例清单 + 强制"显式声明适用条件"？ | 章 5 的形态与可操作性 | 倾向**举例 + 强制显式声明**（判据本身若过硬会变成新教条） |
| **C3** | §9 十一项中**哪些必填、哪些可标"待定"**？ | W3「模型条目最小结构」的可用性 | 倾向区分"最小必填集"（问题类型、机制、适用范围、精度、版本）与"可后补项" |
| **C4** | §11 的旧裁决依赖**记录粒度**：逐条记录，还是按能力段落记录？ | 章 8 的成本与可追溯性 | 倾向**按能力段落 + 例外逐条** |
| **C5** | §4.2 / §4.3 / §6 的"**参数属世界状态还是属当前裁决实例**"是否与"模型 / 状态归属谓词"**合并成一页**处理？ | 章 1 的结构（避免两个相似谓词各说一套） | 倾向合并 |
| **C6** | 是否把需求原文 **§3（按需扩展六步 + "没有真实裁决需求时不主动扩展"）立为方法主文档的「总流程」章**（置于 §4 之前）？ | 方法是否有单一主线；直接关系"防空转" | 倾向**立为总流程章** |
| **C7** | 是否在方法内设立一节**「对需求原文假设的异议与替代方案」**（落实 §13 末段的授权）？ | 方法能否反过来修正需求；与"不越权"的边界 | 倾向设立，但须写明"只提异议，不改需求" |
| **C8** | "未定义"（§7.3）与"冲突"（§7.5）的**登记位置**：方法内建一处清单，还是沿用下游既有的 `_待定登记.md` 一类登记？ | 方法是否规定下游仓库结构（越界风险） | 倾向**只要求"可登记"并给出最小字段**，不指定下游路径 |

### W1 出口判定

| 出口条件 | 结论 |
|----------|------|
| 映射表覆盖 §4～§11 与 §13 全部条款 | **满足**：17 行条款级映射 + 附表 A（§9 十一项）+ 附表 B（§13 十项） |
| 每条有处置与依据 | **满足**：处置取自四值；依据列为条款自身或所登记的候选 |
| 需研究 / 需取舍的点已登记为信息项或决策候选 | **满足**：`C1`～`C8` 登记为 `I-205`；另登记差异 `E1`～`E3` |

**W1 记完成。** 但须说明：本表中标注"需取舍"的行，其**结论尚未产生**（属 `I-205`）；W1 满足的是"映射与登记"这一层面，**不表示方法内容已定**。`I-205` 的最晚需要阶段为 **W2 冻结前**——在它关闭前，方法正文不得就 `C1`～`C8` 写出未裁定的倾向。

### 未选方案

- **把 §1～§3 判为"不在范围"而不登记**：未选。那样方法会缺少"按需扩展"这一主线，正是空转的前兆（登记为 `E1` / `C6`）。
- **把 §9 十一项全部设为 W3 必填**：未选。会造出创作者无法一次填完的表单，诱发补丁或弃用（登记为 `C3`）。
- **由助手直接给出 `C1`～`C8` 的结论**：未选。违反 [`D-003`](D-003-executor-boundary-revision.md) 第 1～3 项（结论须由创作者作出）；本条只登记候选与倾向提示。
- **在 W1 内直接起草方法正文**：未选。方法正文属 W2；W1 只产出映射、差异与候选。

### 影响

- 本目标 `progress`：**0% → 25%**（1/4）。Root `progress` **不变**（仍 25%，1/4）——R2 未完成。
- 新增信息项 `I-205`（`C1`～`C8` 的创作者裁定），`required`，最晚 **W2 冻结前**。
- 运行主记录追加 [`events.md`](../../../../../runtime-records/WRK-002-world-model-method-and-tools/events.md) EV-007；状态保持**「响应中」**。
- **未发生**：方法主文档、两个最小结构、适用性核对均未开始；交付、收件、验收与退出均未发生。

### 后续

- 事实见 [`E-004`](../02-execution/E-004-w1-clause-mapping.md)。
- 下一动作：`I-205` 的裁定（可一次性给口径），随后进入 **W2**（方法主文档形成；草稿落本目标 `attachments/`）。
